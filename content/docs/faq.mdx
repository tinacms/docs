---
cmsUsageWarning: 'https://github.com/tinacms/tinacms/blob/main/packages/tinacms/README.md'
title: FAQ
alias: FAQ
last_edited: '2026-02-09T05:31:49.762Z'
---

<accordionBlock
  accordionItems={[
    {
      heading: "1. What is TinaCloud?",
      docText: "TinaCloud is a hosted [datalayer](/docs/reference/content-api/data-layer) for TinaCMS. TinaCloud is the easiest way to use TinaCMS in production. It provides:\n\n* A hosted GraphQL endpoint for your content\n* Git integration\n* User management\n* [Editorial Workflow](/docs/drafts/editorial-workflow) features\n\nIt also allows authorization for different types of users (content creators, editors, marketers, etc) to login and edit their site without needing to grant direct access to the repository in GitHub.\n\nTinaCMS can be [self-hosted](/docs/self-hosted/overview) or used independently of TinaCloud as well.\n\nFor more information, see [TinaCloud Overview](/docs/tinacloud/overview).\n"
    },
    {
      heading: "2. Where can I get help or share an idea for TinaCMS or TinaCloud?",
      docText: "Check out our communities at [tina.io/community](https://tina.io/community).\n"
    },
    {
      heading: "3. Do I need a certified person to use TinaCMS?",
      docText: "Anyone can start using TinaCMS right away but it is designed for developers to setup and customize. A [certified developer](https://www.credly.com/org/superior-software-for-windows-ssw/badge/tinacms-developer) can accelerate the setup, fine-tune the configuration, and ensure best-practice architecture.\n"
    }
  ]}
/>

## 4. What versions of Node are supported?

<accordion
  heading="Answer"
  docText={<>
    TinaCMS is supported on all active LTS versions of Node. See [Node.js Releases](https://nodejs.org/en/about/previous-releases) to find out more about release versions.

    NPM, PNPM, Yarn and Bun package managers are all supported. We recommend **PNPM**, which is the default in `create-tina-app` and the package manager we use to develop TinaCMS.

    | Node Version |                                          |
    | ------------ | ---------------------------------------- |
    | v26.x        | ℹ️ Not supported - current but not LTS   |
    | v24.x LTS    | ✅ Supported - recommended for production |
    | v23.x        | ❌ EoL                                    |
    | v22.x LTS    | ✅ Supported - good for production        |
    | v21.x        | ❌ EoL                                    |
    | v20.x        | ❌ EoL                                    |
    | v19.x        | ❌ EoL                                    |

    Table updated May 2026
  </>}
/>

<accordionBlock
  accordionItems={[
    {
      heading: "5. What is TinaCMS's tech stack?",
      docText: "### TinaCloud\n\n* Content querying + indexing - **Node**\n* Cloud provider - **AWS**\n* Identity management - **Go**\n\n### TinaCMS\n\n* CMS - **React 18 / React 19**\n"
    },
    {
      heading: "6. What features are unavailable in self-hosted TinaCMS compared to TinaCloud?",
      docText: "When comparing TinaCloud with self-hosting TinaCMS, there are specific features unique to TinaCloud. These include:\n\n### 1. Git Backed Media\n\nIn TinaCloud, there's a Git backed media feature. This integrates media into the TinaCMS Media Manager and commits it directly to the Git repository. However, this functionality is not present in self-hosted TinaCMS.\n\nThe reason for its absence is due to the process involving media uploads. When using the media manager in TinaCloud, images are uploaded to TinaCloud servers and distributed via a CDN. This ensures that images are accessible through the TinaCMS Media Manager, as they require a URL for accessibility. Without this, images won't appear in the Media Manager until the site is rebuilt.\n\n### 2. Dynamic Branch Switching at Runtime\n\nTinaCloud allows for changing the content branch at runtime, a feature managed through URL modification. However, in self-hosted TinaCMS, changing branches is restricted to the build phase only.\n\nThis limitation means that commands like...\n\n```ts\nclient.queries.page({ relativePath: 'home.mdx' }, { branch: 'main' })\n```\n\n...are not functional in a self-hosted TinaCMS setup.\n\n### 3. Search\n\nThe self-hosted backend does not currently have endpoints to support search functionality.\n"
    },
    {
      heading: "7. How to enable co-author on Git commits?",
      docText: "By default when using TinaCloud, the author on Git commits is the [TinaCloud GitHub app](https://github.com/apps/tina-cloud-app). Some users prefer to know who made the change.\n\nFor this use case, TinaCMS allows two different methods:\n\n1. GitHub authoring in order to attribute actions to a GitHub account.\n2. [Co-author](https://docs.github.com/en/pull-requests/committing-changes-to-your-project/creating-and-editing-commits/creating-a-commit-with-multiple-authors) Git commits in order to attribute the commit to an additional user.\n\nBoth of these can be enabled per-user in the TinaCloud account settings. Find out more about [Git Authoring](/docs/tinacloud/git-co-authoring).\n"
    },
    {
      heading: "8. Why do I need a database when using markdown?",
      docText: "A database is essential when using markdown with TinaCMS because the Data Layer, which serves Markdown and JSON files, relies on a database to perform various content management functions. While the Markdown files are the primary source of your content, the database acts as a cache to enable advanced features like search, pagination, and cross-referencing between files. It also provides an API, allowing content fetching akin to traditional headless CMS operations.\n\nInitially, TinaCMS's Data Layer operates unnoticed during local development and is managed by TinaCloud in production. With the introduction of self-hosted options, users can now opt for more control and customization, integrating their own authentication systems and hosting. The entire TinaCMS, including its Data Layer, is open-source, allowing for community-driven enhancements and support.\n"
    },
    {
      heading: "9. What is \"Local Mode\" vs \"Prod Mode\"?",
      docText: "### Local Mode\n\nWhen developers are developing locally, it's often convenient to load/save content from their local filesystem rather than connecting to the content on TinaCloud.\n\nWhen in local-mode, you **will not** need to login to enter edit-mode.\n\n> **Note:** Local-mode is meant for developing locally, and will not work when your site is hosted on production. When in local-mode, TinaCMS tries to hit `http://localhost:4001`, which isn't available at runtime on your production site (and neither is the underlying filesystem content).\n\n### Prod Mode\n\nOnce you are ready to host your site in production and put editing behind authentication, you can self-host the CMS backend yourself or you can use TinaCloud's hosted backend.\n\nWhether you're [self-hosting](/docs/self-hosted/overview) TinaCMS or using [TinaCloud](/docs/tinacloud/dashboard/), TinaCMS's Content API authenticates directly with GitHub removing the need for users to create GitHub accounts. Any changes that are saved by your editors will be committed to the configured branch in your GitHub repository.\n"
    },
    {
      heading: "10. Does TinaCloud only work with GitHub repositories?",
      docText: "Currently yes, the first Git provider that TinaCloud integrates with is GitHub. Other Git providers may be available in the future.\n"
    },
    {
      heading: "11. Does TinaCloud work with Monorepos?",
      docText: "It does! TinaCloud can work with sites inside monorepos by specifying the path to your `tina` folder in your project configuration.\n\nIf your repo is not a monorepo, there's no need to do any configuration. We'll expect your `tina` folder at the root of your repo.\n\nHere's an example monorepo structure that works with TinaCloud:\n\n```\n    /projects/site-a\n    /projects/site-b\n    /projects/site-c\n```\n\nSee [Path To TinaCMS](/docs/tinacloud/dashboard/projects/#path-to-tina) for more information.\n"
    },
    {
      heading: "12. Why does Tina.io login window doesn't close when logging in from a site?",
      docText: "When a user logs in from your site, we will pop open a login window. When login is complete, we will attempt to send a message back to the main window.\n\nThe most common reasons for this issue are:\n\n* The Site URL is not properly set for the TinaCMS project. The main window's base URL will need to match the TinaCMS project's Site URL setup in the TinaCloud Dashboard.\n* The Client ID setup in your site's environment variables does not match the Client ID in your project's settings on the TinaCloud dashboard.\n* The user attempting to login to TinaCloud does not have access to edit this site. Ensure that this user is authorized on the TinaCloud dashboard.\n\n> Make sure to include `https` in the Site URL eg: `https://example.com` or if you are testing locally, it might be something like `http://localhost:3000`\n\n### Site is building on a sub-path\n\nThere is a known limitation where TinaCMS doesn't load assets correctly when the admin is deployed to a sub-path: (`https://jamespohalloran.github.io/my-site-root/admin/` for example). To resolve the issue, in your [TinaCMS config file](/docs/reference/config/), set the `build.basePath` property to value of the sub-path (e.g. `my-site-root`).\n"
    }
  ]}
/>

## 13. How do I resolve "Failed loading TinaCMS assets" error?

<accordion
  heading="Answer"
  docText={<>
    This error means that the TinaCMS admin HTML file failed to load the JavaScript bundle. This generally happens for a few reasons:

    ### User has pushed the development admin/index.html to production

    When you run `tinacms dev` locally, TinaCMS will generate a development admin/index.html file, which loads its assets from localhost. For production, this file should be built in CI using `tinacms build`. If a developer manually removes the admin/index.html file from their `.gitignore`, they may run into this issue.

    ### Your site is served on a different base path

    If your site is served at a sub-directory, ensure that your `build.basePath` is [configured appropriately](/docs/reference/config/#definition)
  </>}
/>

<youtube embedSrc="https://www.youtube.com/embed/9O6gCsxV4Ms?si=451MBmVoTsxXu7X7" caption="FAQ - Setting a base path for TinaCMS" minutes="3" />

<accordionBlock
  accordionItems={[
    {
      heading: "14. What are the common TinaCloud errors?",
      docText: "See the TinaCloud [troubleshooting](/docs/tinacloud/troubleshooting) page for more information.\n"
    },
    {
      heading: "15. How to to access local GraphQL server when it's not running or in prod?",
      docText: "### In production\n\nMake sure your API URL isn't set to point at your local GraphQL server when in production. You should be pointing to the ContentAPI.\n\n### Locally\n\nIf you are working locally, make sure your [GraphQL server is running](/docs/graphql/cli/).\n"
    },
    {
      heading: "16. Vercel Deployment Issues -  What are the common issues when deploying starter templates?",
      docText: "### 1. Popup blocker prevents Vercel from installing their GitHub app\n\nAfter choosing which starter template to deploy and being redirected to Vercel, a popup for Vercel to add their GitHub app may fail to open. Look in the bottom left corner for a toast message saying \"The popup to install the GitHub App could not be opened.\"\n\n![](/img/docs/introduction/vercel-popup-blocked_ogt6fr.png)\\\n**Figure: Popup blocked**\n\nTo fix, check your browser's address bar for blocked popups, allow them, and retry.\n"
    },
    {
      heading: "17. Is TinaCMS LLM-friendly?",
      docText: "Yes. TinaCMS is designed to be highly LLM-friendly, mainly because it uses Markdown for structuring content.\n\nMarkdown gives Large Language Models (LLMs) clear advantages:\n\n* Clear structure – headings, lists, and code blocks are easier to parse\n* Semantic meaning – formatting adds clarity and context\n* Prompt-friendly – consistent markup reduces ambiguity\n* Readable for humans & machines – raw Markdown works well for both\n* Aligned with LLM trends – Markdown fits naturally with the shift toward LLM-optimized content, as highlighted in [SEO is Dead, Long Live LLMO](https://web.archive.org/web/2024/https://jina.ai/news/seo-is-dead-long-live-llmo)\n"
    },
    {
      heading: "18. Do you support Git LFS?",
      docText: "Unfortunately we do not support Git LFS at this time due to limitations with the GitHub API.\n"
    }
  ]}
/>

## 18. Getting a 503 Service Unavailable error when loading the reference field.

<accordion
  heading="Answer"
  docText={<>
    When using the reference field on a collection with a large number of items, you may find the network requests time out. Currently, we do not have anything in place to paginate the reference field.

    The alternative would be to add a [custom field component](https://tina.io/docs/extending-tina/custom-field-components) with pagination. To retrieve the paginated results, you can use a [custom query](https://tina.io/docs/data-fetching/custom-queries).
  </>}
/>

<youtube embedSrc="https://www.youtube.com/embed/6FVpOvLK51s" caption="FAQ - Getting a 503 Service Unavailable error when loading the reference field" />

<accordionBlock
  accordionItems={[
    {
      heading: "19. Do I need a GitHub account to edit?",
      docText: "No! Only the individual who sets up the TinaCloud project, linking it to a GitHub repository, requires GitHub.\n\nEditors can create a [TinaCloud](https://app.tina.io) account with just their email. If co-authoring is enabled for the editor, then commits will be co-authored by their email address.\n"
    },
    {
      heading: "20. GitHub Copilot branches not indexed by TinaCloud",
      docText: "Due to a current limitation in GitHub Copilot’s webhook behaviour, branches created through GitHub Copilot assignments are not automatically indexed by TinaCloud. These branches do not trigger the branch creation webhook that TinaCloud relies on to begin indexing content.\n\nWe have raised this issue with GitHub and will provide updates as they become available in this [Issue](https://github.com/tinacms/tinacms/issues/6407).\n\nAs a temporary workaround, branches created by GitHub Copilot can be manually re-indexed from the [TinaCloud dashboard](https://app.tina.io/).\n\n![](/img/docs/faq/renaming-copilot-branches.gif \"Re-indexing GitHub Copilot branches in Tinacloud\")\n"
    },
    {
      heading: "21. How do I rename media files in the Media Manager?",
      docText: "Renaming media files is not currently supported through the TinaCloud Media Manager. Repo-based media only tracks a single branch and does not have branching functionality for media operations like renaming. If the tracked branch is protected, uploading a renamed file will save it to storage (S3) but the change will not be committed to the branch.\n\nAs a workaround, you can rename media files by making the changes directly in GitHub:\n\n1. Delete the old file from your media directory (e.g. `public/uploads/`) directly in GitHub\n2. Wait for the deletion to sync back to TinaCloud\n3. Add the file with the new name directly in GitHub\n4. Wait for the addition to sync back to TinaCloud\n\nFor more details, see [Renaming Media Files](/docs/reference/media/repo-based/#renaming-media-files).\n"
    },
    {
      heading: "22. Why does my npm build fail with \"Missing field `tsconfigPaths`\" (or Vite version errors)?",
      docText: "This happens under **npm** when two major versions of Vite end up in the dependency tree — for example, Vite 8 pulled in by `@tailwindcss/vite` alongside the Vite 7 that Astro uses. The mismatched rolldown bindings crash the build.\n\nWe recommend **pnpm**, which resolves to a single Vite version automatically and avoids this class of issue. If you must use npm, delete `node_modules` and `package-lock.json`, reinstall, and pin Vite with an `overrides` block in `package.json`:\n\n```json\n\"overrides\": { \"vite\": \"^7\" }\n```\n"
    }
  ]}
/>
