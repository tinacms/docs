---
title: Text Field
---

<Callout variant="info"
  body={<>
    This is an advanced-use feature, and likely not something you'll need to configure. What you probably want is the content types reference!
  </>}
/>

The `text` field represents a single line text input. It should be used for content values that are short strings: for example, a page title.

![tinacms-text-field](/img/fields/text.png)

## Options

<propertyTable
  property={[
    {
      name: "component",
      type: "'text'",
      description: "The name of the plugin component. Always `'text'`.\n",
      required: true
    },
    {
      name: "name",
      type: "string",
      description: "The path to some value in the data being edited.\n",
      required: true
    },
    {
      name: "label",
      type: "string",
      description: "A human readable label for the field. Defaults to the `name`.\n"
    },
    {
      name: "description",
      type: "string",
      description: "Description that expands on the purpose of the field or prompts a specific action.\n"
    },
    {
      name: "placeholder",
      type: "string",
      description: "Placeholder text to appear in the input when it empty.\n"
    }
  ]}
/>

> This interfaces only shows the keys unique to the text field. Visit the [Field Config](/docs/extending-tina/custom-field-components/#default-field-plugins "custom field link") docs for a complete list of options.

## Example: Blog Post Title

Below is an example of how a `text` field could be used to edit the title of a blog post.

```javascript
const BlogPostForm = {
  fields: [
    {
      component: 'text',
      name: 'title',
      label: 'Title',
      description: 'Enter the title of the post here',
      placeholder: '...',
    },
  ],
}
```
